node_modules
.idea
*.log
.nyc_output
test-target
coverage
benchmark-tests
.DS_Store*
npm-debug.log*

tmp
temp
tmp/worktrees
temp/worktrees

# Plaintext environment and private age material must never be committed.
.env
*.env
**/*.env
env/dec/
*.agekey
keys.txt

# The schema and SOPS ciphertext are safe to commit.
!.env.example
!env/enc/*.env.enc
